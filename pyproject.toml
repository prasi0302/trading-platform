[project]
name = "trading-application"
version = "0.1.0"
description = "Real-time stock trading web application"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["services/*/tests", "libs/*/tests"]
